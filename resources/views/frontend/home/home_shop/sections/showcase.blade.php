<!-- banner related area end -->
<div class="tp-shop-banner-area mt-90">
    <div class="tp-shop-banner-wrap">
        <img class="tp-shop-banner-img" data-speed=".8" src="{{ asset($default_content?->image_1) }}" alt="{{ __('frontend.products') }}">
        <div class="tp-shop-banner-sm-img">
            <img data-speed=".8" src="{{ asset($default_content?->image_2) }}" alt="{{ __('frontend.products') }}">
        </div>
    </div>
</div>
<!-- banner related area end -->
